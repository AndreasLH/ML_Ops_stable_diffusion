from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A project for the DTU course Machine Learning Operations. The primary focus is on MLOps and not the models specifically',
    author='Group Awesome 21',
    license='MIT',
)
